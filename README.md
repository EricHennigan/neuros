# neuros
EEG biofeedback symphony

# Install
```bash
pip install .
```

# Install (editable for development)
```bash
pip install -e .
```

# Distribution build
```bash
python -m build --sdist
```

# Test
```bash
python -m pytest tests/
```
